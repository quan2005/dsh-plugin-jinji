# dsh-plugin-jinji 组合包层：插入双半行（Host 数据路由 + 浏览器记忆面板）。
# 安装进 profile 后本层自动应用。默认读取 dsh 进程工作目录下的 .journal；
# 在 profile 的 cordis.patch.yml 里按 id 覆盖 config.root 可指定日志库根目录。
- insert:
    - id: jinji-memory
      name: dsh-plugin-jinji
